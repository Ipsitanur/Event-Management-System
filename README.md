# Event-Management-System
A console-based Event Management System in C with separate Admin and User roles -the Admin handles event and service data by adding, viewing, updating and deleting information. The User can look through categories pick services according to their choice, reserve events check bills and leave feedback.
